{
  "name": "music-enrichment-web",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend build tooling for the Listening Atlas dashboard (web/).",
  "scripts": {
    "build": "node scripts/build_frontend.mjs",
    "dev": "node scripts/build_frontend.mjs --watch",
    "test": "node --test web/data-processing.test.js web/graph-layout.test.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
